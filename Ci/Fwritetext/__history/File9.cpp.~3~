#include "stdafx.h"
#include <iostream>
#include <windows.h>

int main()
{

	char character;
	int number = 51;
	int count = 0;
	ifstream in_stream1;   //для целых чисел
	ifstream in_stream2;    //для символов
	ofstream out_stream;

	out_stream.open("int.txt");//создание файла
	for (count = 1; count <= 5; count++) 
	{
		out_stream << number++ << ' ';
	}
	out_stream.close();

	in_stream1.open("int.txt");                   //подсчет целых чисел в файле
	count = 0;
	in_stream1 >> number;
	while ( !in_stream1.eof() )
	{
	count++;
	in_stream1 >> number;
	}
	in_stream1.close();
	cout << "There are " << count << " int numbers,\n"    ;

	in_stream2.open("int.txt");				//подсчет символов
	count = 0;
	in_stream2 >> character;
	while ( !in_stream2.eof() )
		{
		count++;
		in_stream2 >> character;
		}
	in_stream2.close();
	cout << " that consist of " << count << " chars. \n";
	system("pause");

	return 0;
}

